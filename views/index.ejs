<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
  	<% if(medias) { %>
		<ul>
			<% for(var i=0; i<=medias.length; i++) { %>
				<li><%= medias[i].title %></li>
			<% } %>
		</ul>
  	<% } %>
  </body>
</html>